# 首版交付记录

2026-09-17：Windows x64 首版安装包已生成：`release/SRE Workbench Setup 0.1.0.exe`（150,963,665 字节，约 144 MiB）。随附完整源码、锁定依赖、使用说明和四类示例。

安装包 SHA256：`21D416E1F3E5DA695539A57BA27B35D268EB1EFB80583C0B594F960409833259`。

已实际启动 `release/win-unpacked/SRE Workbench.exe`，打开总览、脚本编辑器和设置页面，程序正常退出。此记录仅确认打包后桌面启动及这些页面可打开，不替代远端功能验收或安装升级验证。

功能实现：中文 Windows 桌面界面、主机与凭据管理、SSH/SFTP、资源面板、系统服务/容器操作、脚本版本、远端任务、源码部署和回退、Prometheus 监控与告警、通用模型及 HTTP Agent 接口。

用户要求先完成项目再实际调试，本轮未执行 Vitest 或 Playwright 测试套件。已经执行构建，并实际打开桌面应用查看总览、Monaco 编辑器和设置页。不能据此推断所有远端功能已完成验收。

后续实际使用调试顺序：

1. 在非生产 Linux 主机完成 SSH 指纹确认、终端、文件上传与资源查看。
2. 部署 examples 下的前端、Node.js、Python 示例，观察成功、失败和版本回退。
3. 增加第二台主机，安装集中监控，确认 Grafana 指标、邮件/Webhook 告警及恢复通知。
4. 接入自己的模型 API 或运行 examples/agent，生成并确认执行只读脚本。
5. 实际中断 SSH、关闭桌面软件及取消任务，检查任务中心核实结果。

暂未完成：三种发行版的真实主机联调、公网证书申请、真实通知渠道验证、Windows 安装升级全过程验证。当前机器的 WSL 注册环境因缺少虚拟化支持无法启动。安装包未商业签名。
