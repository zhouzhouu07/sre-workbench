# 下一轮修改交接：监控问题与 AI 能力深化

记录日期：2026-09-20。用途：继续测试和开发时，先读最新交接状态；下方早期需求保留追溯，不能覆盖最新验收结果。

## 最新交接状态（优先于下方早期计划）

- 2026-09-21：已安装 frontend-app-builder / frontend-testing-debugging（用户 Codex 技能目录，来自 openai/plugins），完成平台统一视觉优化：深蓝导航、总览指标信息栏、青绿 AI 入口、对话和明暗主题、窄窗口布局。v0.2.2 已公开发行，源码标签 225b58f，安装包与 blockmap/SHA256 附件均通过服务端摘要核对；129 项单元测试、6 项 Electron 测试和实际打包程序启动通过；未修改远端系统与 AI 权限逻辑。

- v0.2.1 已于 2026-09-21 [公开发布](https://github.com/zhouzhouu07/sre-workbench/releases/tag/v0.2.1)，源码标签对应 `e5b9a65`，安装包/校验文件上传成功。修复 DeepSeek Anthropic/deepseek-flash 多 JSON/DSML 响应，使用原生单个工具调用；真实环境检查 6 步完成。AI 改为服务器专用对话界面，支持命名、暂停/继续和持续聊天；AI 记录从任务中心隐藏，在会话内管理。129 项单元、6 项 Electron 及类型/构建通过，实际打包程序启动验收通过。详见 [会话修复验收](testing/2026-09-20-ai-chat-fixes.md)。

- 2026-09-20 最新验收：AI 真实 DeepSeek 自主部署 Nginx + Flask 博客已完成，独立浏览器检查首页/两篇文章/样式正常；测试项目 `/opt/sre-ai-blog-97e37011`，端口 18085，保留运行。原有 14 个容器清单一致，18084 返回 200。
- 最终类型检查、119 项单元测试、5 项 Electron 测试、生产构建通过；v0.2.0 `.exe` 已生成，打包程序启动检查确认四档 AI 权限和监控修复在包内。[v0.2.0 已公开发布](https://github.com/zhouzhouu07/sre-workbench/releases/tag/v0.2.0)，安装包、blockmap、SHA256 附件上传成功，服务端摘要与本地一致。
- 当前任务界面仅选择 SSH Linux 服务器，保留四档权限、多轮工具执行、审批/暂停/继续/停止与持久记录；原脚本助手保留为兼容入口。仍未实施 Agent/工作流/Skill 工作台、桌面/浏览器自动控制；Windows 覆盖升级尚未验证。
- 用户要求每项操作后追加根目录 `项目进程.md`，它是统一操作记录。用户已授权本轮真实测试以及完成后的打包与 GitHub 发行，无需再次询问。
- 后续用户已授权并完成监控模块源码修复：自定义初始用户名、三个管理端口、实例归属核验、部署前冲突检查、旧账号行为提示、保存配置关闭旧隧道。96 项单元测试、4 项界面回归、类型检查与构建通过；实机新账号登录、自定义端口链路和同方案重复部署通过。
- 本轮 AI 和监控修复源码已提交为 `b303686`，已推送 `feat/sre-desktop` 和 `v0.2.0` 标签；旧 v0.1.2 包不包含本轮修复。
- 已认证查询确认 v0.1.2 Release 是无附件草稿，未公开发布，本轮保留该草稿。
- 本轮隔离监控测试实例已停止，数据卷保留；此前旧监控和用户 node-demo（18084）仍运行。旧 Grafana 密码未重置。没有实现旧实例接管或密码恢复向导。
- 用户要求整理目录。约 975.19 MB 的旧安装包、重复打包目录、下载缓存、临时编译脚本和测试输出已移动到根目录 `待清理文件/`，未删除、未释放磁盘空间。目录保留原子路径，已加入 `.gitignore`。
- 保留源码、依赖、dist、release/win-unpacked、v0.1.2 安装包、vendor/git、测试脚本源码以及 `.tools/vm-test-data`。部分 `.tools/*.mjs` 临时编译产物已经归档，运行前应从对应 `.ts` 重新 bundle，不能假定旧命令仍可直接执行。
- 浏览器/完整桌面操作，以及设置中的自定义工作流、搭建 Agent、导入 Skill 仍未实现；不要与本轮文件/终端任务助手混淆。下方旧需求保留追溯，以本节及 `项目进程.md` 为准。

### 新对话推荐阅读顺序

1. 本文件：当前状态、需求、工作方式与后续方向。
2. `docs/testing/2026-09-20-monitoring-issues.md`：监控问题现场证据。
3. `docs/testing/2026-09-20-monitoring-fixes.md`：已完成修复与验收，避免重复开发。
4. `docs/user-guide.md`：当前源码的使用方式和边界。
5. `docs/testing/2026-09-19-rocky-integration.md`：既有实机验证与未覆盖范围。
6. `docs/api-features.md`、`docs/api-core.md`、`docs/agent-api.md`：功能、核心和外部 Agent 契约；AI 设计前再读架构博客 `docs/blog/2026-09-17-sre-workbench-architecture.md`。

先运行 `git status --short`、`git diff --stat`，按任务读取相关源码，不要重新执行所有远端测试或直接清理旧实例。README 和界面版本已更新为 0.2.0，发行状态仍以 GitHub API 实际查询为准。

## 用户目标与工作方式

- 项目准备参加 AI + 软件比赛；在现有 SRE 运维工作台基础上深化 AI 功能，形成能够演示实际任务完成过程的产品。
- 当前先测试、记录问题，后续集中修改。不要因读到本文件就自行开始发布或操作电脑。
- 现有部署、脚本、监控等能力应保留，AI 应与真实工具和任务反馈结合。
- AI 接到需求后应严格围绕需求工作；权限高不代表可扩展任务范围。记录执行过程、结果和未完成项，以验证结果为准。

## 已有版本与测试背景

- 仓库：https://github.com/zhouzhouu07/sre-workbench
- v0.1.2 源码和标签已推送，提交 `4dbd03c`；Windows 安装包构建完成。此前 Release 附件上传被对话打断，**尚未确认最终公开发布状态**。下一轮应先查询 GitHub 实际状态，避免重复发布或覆盖附件。
- 最近代码回归：92 项单元测试、4 项 Electron 测试通过，类型检查通过。Rocky Linux 9.4 四类模板部署、回退、失败保护、任务恢复、监控采集及测试告警/静默已实测。
- 测试报告：[Rocky 实机联调记录](testing/2026-09-19-rocky-integration.md)。测试通过不能代替真实登录、安装升级和邮件送达验证。
- 当前虚拟机有此前测试留下的应用和监控实例。处置前核对归属，不直接清空 Docker 或持久化数据。凭据不写入交接文档。

## 第一批：监控问题集中处理

2026-09-20 进度：已在本地实现自定义端口、初始用户名、实例归属核验和冲突提示，完成实机登录及自定义端口链路验证；见 [本轮修复验收](testing/2026-09-20-monitoring-fixes.md)。尚未更新安装包/发布。下面原始问题及需求保留作追溯；不要将已实现项重复当作待开发。旧实例接管、密码恢复向导未实现，仍需使用原方案和已有账号。

现场证据见 [2026-09-20 监控问题记录](testing/2026-09-20-monitoring-issues.md)。

### 已确认的问题

1. 旧测试 Grafana 占用远端回环地址 3000，所属项目为 `sre-mon-36d81a08-57ea-44c8-9ae3-c4d866fdcb06`；9090、9093 同属旧实例。
2. 用户监控部署报 `Port occupied by unmanaged service: 3000`。代码按方案 ID 判断归属，另一个工作台方案被笼统标为 unmanaged。
3. 浏览器打开旧 Grafana，登录失败；日志明确为 HTTP 401、invalid password，健康检查正常。新部署失败，不能认为新方案密码已经生效。
4. Node Exporter 使用共享项目，采集器任务成功不等于监控主服务部署成功。
5. 此前测试只验证页面访问与健康状态，没有验证真实浏览器登录，也未覆盖测试遗留实例与用户新方案冲突。

### 用户提出的修改目标

- 增加 Grafana 用户名填写，说明初始账号与已有数据卷账号的区别。
- 增加监控端口选择；建议高级设置提供 Grafana 3000、Prometheus 9090、Alertmanager 9093 的默认值，具体 UI 后续设计。
- 端口变化必须同步到部署、预检、健康检查、SSH 隧道、状态查询、测试告警及静默等全部调用。
- 保留管理端口回环绑定和 SSH 隧道访问；明确服务器映射端口与容器内部端口的区别。
- 识别旧实例并给出可理解的归属信息；打开页面前检查当前方案是否对应实际实例。
- 明确“复用旧实例”和“另建实例”的操作语义，不靠更换端口掩盖旧实例或账号问题。是否支持同主机多套监控需要正式设计与资源评估。
- 不因重新填写用户名/密码而暗示已有 Grafana 账号会自动更新；需要明确的更新或恢复流程。

验收需覆盖：真实账号登录、空主机首次部署、同方案重部署、不同方案冲突、自定义端口全链路、保留数据卷后的账号行为、SSH 隧道是否指向正确实例。真实 SMTP 邮件送达、阈值告警全链路、跨主机采集、公网证书仍需对应测试条件。

## 第二批：AI 权限分级与任务执行

### 用户明确要求

- AI 模块加入权限程度选择，除其他权限档位外，最高权限允许操作电脑。
- 用户给出需求后，AI 严格按需求执行，而不只是生成文字建议。
- 后续深化自定义工作流、搭建 Agent、导入 Skill 等能力，以支持比赛展示。

### 待设计的权限方案（建议，尚未定稿）

| 候选档位 | 候选能力范围 |
| --- | --- |
| 分析建议 | 对话、分析用户提供的上下文，不操作系统 |
| 只读诊断 | 在选定范围读取状态、日志、文件；不变更业务配置 |
| 确认后执行 | 生成变更方案和命令，由用户确认后调用工具 |
| 范围内自主执行 | 在指定主机、目录和工具权限内完成多步骤任务，展示进度与验证结果 |
| 电脑操作 | 支持用户授权范围内的本地终端、文件，以及浏览器/桌面操作；具体平台和技术路线待确认 |

“操作电脑”究竟包含 Windows 本机、远端 Linux、浏览器还是完整桌面，需要下一轮明确。不能将现有 SSH 执行能力等同于本机桌面控制。

权限应由工具执行层校验，不能仅靠提示词；模型回复、导入的 Skill 或外部文档不能自行提升权限。高权限仍受用户任务范围约束。需要可见的当前权限、执行记录、中止入口、超时及失败反馈；具体逐步确认规则和危险操作边界后续与用户确定，避免每个普通操作都反复确认。

验收方向：不同权限档位下相同工具调用得到正确允许/拒绝结果；无法越权；任务执行可中止；实际完成情况可验证；失败和部分完成如实呈现。不得把“严格遵循需求”宣传成模型永不出错的保证。

## 第三批：工作流、Agent 与 Skill

- **自定义工作流**：用户要求支持。后续讨论步骤编排、输入输出、条件分支、人工确认、失败处理、日志及复用；这些细节为候选设计，不是已确认范围。
- **搭建 Agent**：用户要求支持。后续讨论模型、角色指令、工具、权限、Skill 与工作流如何组合；区分内置 Agent 和外部 HTTP Agent 接入。
- **导入 Skill**：用户要求支持。格式、来源、版本、依赖及兼容性待确定；导入展示能力和权限需求，不能把文档里的指令当作用户授权自动执行。
- **模型接入体验**：用户曾把 DeepSeek 配成外部 HTTP Agent，得到 HTTP 404（agent）。应改善类型说明、示例和错误提示。DeepSeek 应选兼容模型 API；外部 Agent 必须适配本项目的自定义请求/返回协议，不是任意 Agent 平台通用接口。
- **文档与比赛演示**：补充逐步使用案例、权限说明、模型/Agent 区别和操作反馈。候选演示为“提出运维需求 → 诊断 → 工作流执行 → 验证 → 结果报告”，演示范围后续确定。

## 下一轮建议顺序

1. 阅读本文、问题记录和当前代码；查询工作区、GitHub Release、远端已有实例状态。
2. 先处理监控登录、实例归属与端口配置问题，完成对应回归。
3. 与用户收敛 AI 权限和电脑操作范围，形成设计与分阶段实施计划。
4. 再实现工具执行闭环、工作流、Agent 和 Skill，逐项验收，避免一次性堆叠未验证功能。
5. 同步更新使用文档、测试报告与发行说明；发布按届时用户指令执行。

新对话可用提示：**“请先阅读 docs/next-session-handoff.md 和其中链接的问题记录，接着处理监控问题，再设计 AI 权限分级与 Agent/工作流/Skill 能力。先核对当前状态，不重复已经完成的测试。”**
